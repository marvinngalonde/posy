<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 12px;
            line-height: 1.4;
            color: #374151;
            background: #fff;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #1f2937;
        }

        .company-info {
            flex: 1;
        }

        .company-name {
            font-size: 24px;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 5px;
        }

        .company-details {
            font-size: 11px;
            color: #6b7280;
            line-height: 1.3;
        }

        .report-info {
            text-align: right;
            flex: 1;
        }

        .report-title {
            font-size: 20px;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 10px;
        }

        .report-meta {
            font-size: 11px;
            color: #6b7280;
        }

        .report-meta div {
            margin-bottom: 3px;
        }

        .content {
            margin-bottom: 30px;
        }

        .summary-section {
            margin-bottom: 25px;
            padding: 15px;
            background: #f9fafb;
            border-radius: 6px;
            border: 1px solid #e5e7eb;
        }

        .summary-title {
            font-size: 14px;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 10px;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
        }

        .summary-item {
            text-align: center;
        }

        .summary-item .label {
            font-size: 10px;
            color: #6b7280;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 3px;
        }

        .summary-item .value {
            font-size: 16px;
            font-weight: bold;
            color: #1f2937;
        }

        .table-container {
            overflow-x: auto;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 11px;
        }

        th {
            background: #1f2937;
            color: white;
            padding: 8px 6px;
            text-align: left;
            font-weight: 600;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        td {
            padding: 6px;
            border-bottom: 1px solid #e5e7eb;
            vertical-align: top;
        }

        tr:nth-child(even) {
            background: #f9fafb;
        }

        tr:hover {
            background: #f3f4f6;
        }

        .text-right {
            text-align: right;
        }

        .text-center {
            text-align: center;
        }

        .font-medium {
            font-weight: 500;
        }

        .font-bold {
            font-weight: bold;
        }

        .status-badge {
            padding: 3px 6px;
            border-radius: 3px;
            font-size: 10px;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.3px;
        }

        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            font-size: 10px;
            color: #6b7280;
            text-align: center;
        }

        .page-break {
            page-break-before: always;
        }

        @media print {
            .no-print {
                display: none;
            }

            body {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
        }

        .filters-section {
            margin-bottom: 20px;
            padding: 12px;
            background: #f8fafc;
            border-radius: 4px;
            border: 1px solid #e2e8f0;
        }

        .filters-title {
            font-size: 12px;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 8px;
        }

        .filters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 10px;
            font-size: 10px;
        }

        .filter-item {
            display: flex;
            justify-content: space-between;
        }

        .filter-label {
            color: #64748b;
            font-weight: 500;
        }

        .filter-value {
            color: #1e293b;
            font-weight: 600;
        }

        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #6b7280;
        }

        .empty-state-icon {
            font-size: 48px;
            margin-bottom: 12px;
            opacity: 0.3;
        }

        .empty-state-title {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 6px;
            color: #374151;
        }

        .empty-state-description {
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="company-info">
            <div class="company-name">{{company.name}}</div>
            <div class="company-details">
                {{#if company.address}}{{company.address}}<br>{{/if}}
                {{#if company.city}}{{company.city}}, {{/if}}{{#if company.state}}{{company.state}} {{/if}}{{#if company.postal_code}}{{company.postal_code}}{{/if}}
                {{#if company.phone}}<br>Phone: {{company.phone}}{{/if}}
                {{#if company.email}}<br>Email: {{company.email}}{{/if}}
            </div>
        </div>
        <div class="report-info">
            <div class="report-title">{{title}}</div>
            <div class="report-meta">
                <div><strong>Generated:</strong> {{formatDateTime generatedAt}}</div>
                {{#if dateRange}}
                <div><strong>Period:</strong> {{dateRange}}</div>
                {{/if}}
                {{#if totalRecords}}
                <div><strong>Total Records:</strong> {{totalRecords}}</div>
                {{/if}}
            </div>
        </div>
    </div>

    {{#if filters}}
    <div class="filters-section">
        <div class="filters-title">Applied Filters</div>
        <div class="filters-grid">
            {{#each filters}}
            <div class="filter-item">
                <span class="filter-label">{{label}}:</span>
                <span class="filter-value">{{value}}</span>
            </div>
            {{/each}}
        </div>
    </div>
    {{/if}}

    {{#if summary}}
    <div class="summary-section">
        <div class="summary-title">Summary</div>
        <div class="summary-grid">
            {{#each summary}}
            <div class="summary-item">
                <div class="label">{{label}}</div>
                <div class="value">{{#if isCurrency}}{{formatCurrency value}}{{else}}{{value}}{{/if}}</div>
            </div>
            {{/each}}
        </div>
    </div>
    {{/if}}

    <div class="content">
        {{{content}}}
    </div>

    <div class="footer">
        Generated by POS System on {{formatDateTime generatedAt}} | Page {{pageNumber}} of {{totalPages}}
    </div>
</body>
</html>